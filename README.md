Spring Boot backend: exposes `/api/apps` with sample aggregated data.

To build:

```
cd backend-springboot
mvn clean package
java -jar target/backend-springboot-0.0.1-SNAPSHOT.jar
```

Adjust Java and Maven installation as needed.
